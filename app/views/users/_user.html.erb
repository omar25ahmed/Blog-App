<div class="d-flex border border-secondary">
    <img src="<%= @user.photo %>" alt="user-img" class="img-responsive mx-5">
    <div class="ms-5 d-flex align-center justify-content-center gap-5">
      <h1><%= link_to @user.name, user_path(id: @user.id) %></h1>
      <p class="mt-4 mb-0 ">number of posts: <%= @user.posts_counter || 0 %></p>
    </div>
</div>